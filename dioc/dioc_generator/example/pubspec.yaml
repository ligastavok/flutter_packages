name: dioc_example
publish_to: none

environment:
  sdk: '>2.12.0-0 <3.0.0'
  # sdk: ">=2.7.0 <3.0.0"

dependencies:
  # flutter:
  #   sdk: flutter
  dioc:
    path: ../../dioc


dev_dependencies:
  # analyzer: ^1.0.0
  build: ^2.0.0
  build_runner: ^1.12.1
  # built_value_generator: ^7.0.0
  # pedantic: 1.10.0-nullsafety.3
  # pedantic: 1.10.0-nullsafety.3
  dioc_generator:
    path: ../../dioc_generator

# dependency_overrides:
#   analyzer: ^1.0.0
#   meta: ^1.3.0
#   meta: ^1.3.0
  # collection: ^1.15.0
  # watcher: ^1.0.0
  # pedantic: ^1.10.0